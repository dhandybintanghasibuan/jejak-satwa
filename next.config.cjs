module.exports = {
  plugins: {
    // Mengaktifkan Tailwind CSS untuk memproses semua file CSS
    tailwindcss: {},
    // Menambahkan prefix browser secara otomatis untuk kompatibilitas
    autoprefixer: {},
  },
};
